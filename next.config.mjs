/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', 'img.freepik.com', 'indonesiakaya.com'],
    },
  };
  
  export default nextConfig;
  